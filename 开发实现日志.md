# 投标初稿助手开发实现日志

更新时间：2026-08-05

本文档记录项目已经实现的能力、验证结果、已知边界和下一阶段工作。每次形成可运行增量后追加记录，不以聊天记录代替项目文档。

## 当前版本概览

当前版本已从“可演示 MVP”进入“本地可试用版本”：主链路可以完整运行，项目数据可以归档、备份和迁移，关键分析及正文可以人工修订，并有自动复核和 Word 交付物。

核心原则：

1. 先保证关键条款不遗漏，再优化生成文风。
2. 没有企业资料依据时明确标记待补充，不生成虚假事实。
3. 保留原文页码、引用、人工确认和复核状态，确保结果可追溯。
4. 采用单体 Streamlit、本地文件和 SQLite，控制单人开发与维护成本。

## 版本记录

### V0.1 可演示 MVP

对应提交：`7755b24 feat: initialize bid assistant MVP`

- 建立 Streamlit 单页应用和本地项目目录
- 支持 PDF、DOCX、TXT、Markdown 解析
- 完成规则式招标要求、评分项、资格、材料、时间和风险提取
- 支持 OpenAI 兼容接口的可选分析及章节生成
- 支持企业、产品、历史方案三类知识资料
- 支持章节计划、草稿编辑和固定格式 Word 导出
- 建立第一批单元测试、示例数据和实施路径文档

### V0.2 分析与复核可靠性

对应提交：`016d5a1 feat: add review workflow and long document analysis`

- LLM 长文档按页分段处理，合并去重，并保留部分成功结果
- 分析项保留原文页码和引用，错误引用会降级并提示
- 知识资料解析增加基于文件修改时间和大小的缓存
- 新增自动复核报告及处理状态持久化
- 检查缺失项、未确认条款、评分项追溯、废标风险、缺失章节、占位符、证据缺失和工期/质保冲突
- Word 增加复核报告附录
- 新增 GitHub Actions 编译与测试工作流

### V0.2.1 跨平台稳定性

对应提交：

- `1568cbf fix: normalize uploaded filenames across platforms`
- `678e433 ci: update GitHub Actions runtimes`

实现内容：

- 统一处理 Windows 和类 Unix 路径分隔符，避免上传文件名携带目录
- 升级 GitHub Actions 官方运行时版本

### V0.3 项目生命周期与迁移

对应提交：`43a0d8e feat: add project lifecycle and secure backups`

- SQLite 启动时自动迁移 `archived` 字段，不破坏现有项目
- 项目支持归档、恢复，以及显示/隐藏已归档项目
- 首页显示招标文件、解析、分析、草稿、复核、Word 六阶段进度
- 显示已关联知识资料文件数
- 当前项目可导出完整 ZIP 备份
- ZIP 包含项目元数据、源文件、分析、草稿、知识资料、复核状态和输出文件
- 导入校验格式版本、重复路径、路径穿越、符号链接、加密 ZIP、文件数和解压体积
- 每个备份文件使用 SHA-256 校验完整性
- 项目 ID 冲突时生成新 ID，不覆盖现有项目
- 导入先写临时目录，全部校验成功后再登记项目

### V0.4 一致性复核、草稿版本与 Word 排版

对应提交：`3a50afe feat: add draft history and polished Word export`

- 新增预算/最高限价和投标报价一致性检查
- 金额统一换算为元后比较，避免 `100 万元` 与 `1,000,000 元` 被误判
- 新增投标截止时间和开标时间一致性检查
- 日期时间统一格式后比较，兼容中文日期、横线日期和斜线日期
- 按具体角色检查项目经理、技术人员、售后人员等数量冲突
- 按 ISO、CMMI、ITSS 等资质名称检查有效期冲突
- 新增项目复制，保留业务数据但排除旧 Word 和旧版本历史
- 草稿生成及人工保存自动建立版本快照
- 支持查看版本时间、原因、章节数量和章节名称，并恢复历史版本
- 恢复前自动保存当前正文，恢复后旧复核报告自动失效
- 每个项目最多保留最近 50 个草稿版本，完整 ZIP 备份包含版本记录
- Word 输出统一为中文 A4 投标文件版式，正文使用宋体 10.5 磅、1.5 倍行距和明确段落间距
- 标题使用微软雅黑并设置层级字号、颜色和字距，列表改为 Word 原生项目符号及编号
- 封面项目信息使用固定列宽，启用首页不同并隐藏封面页眉页脚
- 正文页眉和页脚使用固定宽度双列布局，项目名、文件类型、提示语和页码稳定左右对齐
- 表格统一固定宽度、单元格留白、垂直居中、隔行底色和重复表头，禁止数据行跨页拆分
- 自动复核表由六列调整为四列，问题和处理建议合并展示，高风险行使用浅红底色

### V0.5 导出检查与 Word 版本记录

状态：已完成并提交（`183d465`）

- Word 导出页新增项目基本信息、章节完整性、高风险、占位符、证据追溯和复核进度检查清单
- 没有章节草稿时阻止导出；存在未处理风险时要求显式确认文件仅用于内部复核
- 每次导出使用 `V001`、`V002` 递增文件名，不再覆盖旧 Word
- 每个版本记录生成时间、章节数、风险摘要、检查警告数、版本说明、文件大小和 SHA-256
- 界面可查看 Word 历史版本，并选择任意版本下载
- 每个项目最多保留最近 50 个 Word 版本，完整 ZIP 备份自动包含版本文件及元数据
- 浏览器实测完成风险确认、`V001` 生成、流程进度更新、历史版本选择与下载入口验收
- 页面真实生成的 16 页 Word 已逐页渲染检查，封面、目录、正文、页眉页脚和跨页复核表无裁切或重叠
- 自动化回归共 35 项测试通过，`pip check`、`compileall` 和 `git diff --check` 均通过

### V0.6 附件目录与最终提交材料清单

状态：已完成并提交（`0c07371`）

- 新增第七阶段“提交清单”，从分析结果自动同步未忽略的所需材料
- 同步时保留人工状态、附件关联和备注，人工新增项不会被分析同步覆盖
- 清单支持六类材料、必交标记、待准备/已备妥/不适用状态、原文页码和备注
- 新增六类提交附件目录，支持分类上传、下载和移除，同名文件自动追加序号
- 附件删除后自动提示清单断链，最终清单支持 UTF-8 CSV 导出
- 必交项全部完成后，第七阶段才计入项目总进度
- 完整 ZIP 备份包含提交附件和清单，复制项目时排除项目专属提交材料
- 浏览器已验证清单生成、人工保存、附件展示/下载/移除和 CSV 下载链路
- 自动化回归共 38 项测试通过，`compileall` 和 `git diff --check` 均通过

### V0.7 提交包预检与版本化打包

状态：已完成并提交（`b5deab9`）

- 项目流程扩展为八阶段，提交包生成后单独标记“提交打包”完成
- 新增提交包预检，缺少 Word/清单、清单存在未保存编辑、必交项未完成或附件断链时禁止打包
- 未生成复核报告或仍有待处理问题时要求显式确认，仅生成内部预审包
- 支持选择任意历史 Word 版本，并汇总当前已保存清单和六类附件
- ZIP 包含 Word、分类附件、带 UTF-8 BOM 的材料清单、提交包说明和 `package-manifest.json`
- 清单记录包内每个内容文件的大小和 SHA-256，自动化测试逐项复算验证
- 提交包使用 `P001`、`P002` 递增版本号，记录 Word 来源、清单摘要、附件数、风险数、用途和版本说明
- 每个项目最多保留最近 20 个提交包；完整项目备份保留提交包及元数据，项目复制排除旧提交产物
- 浏览器已验证示例项目的必交项阻断，以及一次性验收项目从 `7/8`、`P001` 生成到 `8/8`、历史下载入口的完整链路
- 浏览器生成包内含所选 Word、分类附件、CSV、说明和校验清单，逐文件复算 SHA-256 全部一致；验收项目随后已清理
- 自动化回归共 43 项测试通过，`compileall` 和 `git diff --check` 均通过

### V0.8 提交包完整性校验与校验报告

状态：已完成并提交（`44b36baf`）

- 下载前重新计算提交包外层 SHA-256，发现生成后被改动或替换时阻止下载
- 校验 ZIP 路径安全、重复路径、符号链接、加密内容、文件数量和解压体积
- 对比 `package-manifest.json` 与实际文件集合，识别缺失文件、未登记文件和无效记录
- 逐文件复算大小和 SHA-256，并核对唯一 Word、Word 来源、CSV 编码和必要说明文件
- 检查材料清单完成状态，以及待处理复核问题是否正确标记为内部预审
- 校验成功时显示文件数和内容大小；校验失败时展示错误清单并禁用 ZIP 下载
- 成功或失败均可下载带 BOM 的中文校验报告，包含版本来源、哈希、错误/提示和文件明细
- 校验结果根据文件大小和修改时间缓存，文件变化后自动失效并重新计算
- 浏览器已验证正常包显示校验通过且两个下载入口可用；修改同一 ZIP 后页面自动转为不通过、禁用 ZIP 下载并保留报告下载
- 一次性验收项目和损坏包已在验证后清理，原示例项目数据未被修改
- 自动化回归共 46 项测试通过，`compileall` 和 `git diff --check` 均通过

### V0.9 Word 成品完整性与版式结构质检

状态：已实现并提交，功能提交 `fcd1e5a`

- Word 历史版本下载前重新计算 SHA-256，识别生成后被替换或修改的文件
- 检查 DOCX 必要 ZIP 结构，并使用 python-docx 重新打开验证文档可读性
- 对比版本记录的章节数，章节缺失、文件损坏、哈希变化或非 A4 时禁止下载
- 检查正文宋体 10.5 磅、1.5 倍行距、标题字距、页码字段、首页不同和字段更新设置
- 检查表格固定宽度和正文表格禁止单行跨页拆分
- 识别待补充、待确认、待核对和方括号占位内容，作为需人工确认项
- Word 质检结果接入提交包预检：阻断问题禁止打包，提示项要求仅内部预审确认
- 支持下载带 BOM 的中文 Word 质检报告，包含哈希、结构检查、错误项和提示项
- 历史 Word 被删除或移动时仍能稳定显示阻断结论，不会因读取文件状态导致页面异常
- 浏览器已验证示例 Word 完整且存在 46 处占位提示；修改一次性项目 DOCX 后，Word 下载和提交打包均被禁用
- 一次性验收项目和损坏 Word 已清理，原示例项目未被修改
- 自动化回归共 52 项测试通过，`compileall` 和 `git diff --check` 均通过

### V1.0 真实项目分析验收台账

状态：已实现并提交，功能提交 `0530bee`

- 每次规则或 LLM 分析完成时固化自动分析基线，人工编辑不覆盖原始结果
- 基线来源单独记录；历史项目从当前结果建立的基线只统计后续变化
- 基线保存解析结果指纹，当前解析文件与基线不一致时暂停验收保存和报告下载
- 按稳定条目 ID 统计人工确认命中、忽略/删除误报、人工补充漏项和待确认项
- 记录原提取项被人工修改的数量，便于定位内容或引用需要修正的分析结果
- 计算已审核项命中率；只有全部条目复核完成后才显示覆盖率估算
- 支持记录验收人、纯人工预计耗时、系统协助实际耗时和验收备注
- 分析表格存在未保存修改时，验收记录保存和报告下载自动禁用
- 支持下载带 UTF-8 BOM 的中文验收报告，包含指标、耗时和逐项明细
- 验收基线、来源和记录随项目复制及完整 ZIP 备份迁移
- 浏览器已验证旧项目兼容入口、14 项阶段性样本、75 分钟节省时间、记录落盘和未保存修改保护
- 浏览器已验证历史项目基线来源提示；临时基线和验收记录已全部清理，示例项目业务数据恢复到测试前状态
- Streamlit 已完整重启并重新加载新模块，健康检查通过 `200 / ok`
- 自动化回归共 58 项测试通过，`compileall`、`pip check` 和 `git diff --check` 均通过

### V1.1 模型连接诊断与本地配置

状态：已实现

- 修正默认模型配置，从未落地的 `qwen3.5:9b` 调整为更适合当前 16GB 内存、8GB 显存电脑的 `qwen3:4b`
- 左侧模型配置支持编辑接口地址、模型名称、API Key、请求超时、分段字符数和最大分段数
- 配置可保存到本机 `.env`，密钥不进入 Git、项目 ZIP 或业务数据目录
- 模型健康检查验证 `/models` 接口及目标模型是否存在，区分服务未启动、超时、鉴权失败、模型缺失和响应格式异常
- LLM 分析和章节生成在正式调用前只执行一次健康检查；不可用时立即回退规则模式，避免每个分段或章节重复等待连接失败
- 底层连接错误改为简短、可操作的中文提示，不再把 `HTTPConnectionPool` 和 `WinError 10061` 直接展示给业务用户
- 默认长文档分段调整为每段 12000 字、最多 12 段，并允许按模型上下文和硬件能力配置
- `run.ps1` 检测到本机 Ollama 时可自动启动未监听的 11434 服务
- 新增模型不可达、模型缺失、模型可用和调用连接失败的自动化测试

### V1.2 本地 Qwen3 镜像部署

状态：已实现并完成本机部署

- 使用 ModelScope 官方仓库下载 `Qwen3-4B-Q4_K_M.gguf`，文件大小 2,497,280,256 字节
- 模型 SHA-256 校验通过：`7485fe6f11af29433bc51cab58009521f205840f5b4ae3a32fa7f92e8534fdf5`
- 使用国内 GitHub 代理下载 `llama.cpp b10256` Windows Vulkan 运行时
- 运行时 SHA-256 校验通过：`ea787c151309a80b908809a04b6f71d44da41de0ea4b2794114567b67df861f6`
- Vulkan 后端识别到 NVIDIA GeForce RTX 4060 Laptop GPU 和 AMD Radeon 780M，默认固定使用 `Vulkan0`
- 新增 `setup_local_llm.ps1`，支持镜像下载、断点续传、解压和双重 SHA-256 校验
- 新增 `start_local_llm.ps1`，使用 16384 上下文、GPU 全层卸载、Flash Attention 和关闭思考输出启动服务
- 服务只监听 `127.0.0.1`，启用与 `.env` 一致的本地 API Key，并限制 CORS 来源
- 新增 `stop_local_llm.ps1`，只停止 PID 记录且路径匹配的项目托管进程
- `run.ps1` 优先启动项目内置 `llama-server`，本地文件缺失时才回退到已有 Ollama
- 本地服务沿用 `http://127.0.0.1:11434/v1` 和模型别名 `qwen3:4b`，应用配置无需切换协议
- `models/`、`runtime/` 和 PID 文件已加入 Git 忽略，避免把约 2.5GB 模型提交到仓库
- 真实中文结构化分析验证通过，能够提取项目、采购人、预算、截止时间、强制要求、评分、资格、材料和废标风险
- 增加小模型结构化输出归一化：兼容数字分值、中文置信度、百分比置信度和字符串页码
- 修复重新分析后 Streamlit 输入框保留旧空值、未立即显示新项目基本信息的问题
- 项目基本信息字段增加原文依据提示；采购文件未明确出现采购人、代理机构或投标截止时间时继续留空，避免推测补写
- 知识资料上传新增 CSV 和 JSON；CSV 按表头与记录展开，JSON 保留嵌套字段路径后进入现有检索缓存
- 招标文件上传仍限制为 PDF、DOCX、TXT、Markdown，结构化资料格式只在知识资料板块开放
- 知识资料保存前执行临时解析校验，损坏 JSON、异常 CSV 或无可检索文本的文件不会写入项目目录
- 企业资料、产品资料、历史方案均增加逐文件删除入口；删除前二次确认，成功后同步刷新资料数量和知识检索缓存
- 主流程标签页改为有状态组件，上传、删除、取消等触发页面刷新后继续停留在用户当前标签，不再回到“1. 招标文件”

### V1.3 真实模板、资质图片、MinerU 与混合检索

状态：已完成代码实现和自动化测试，MinerU 本体按需独立安装

- 新增项目级 DOCX 模板上传、下载和删除，非法 DOCX 在写入前拦截
- 支持七类模板占位符，正文插入点不存在时采用文末追加的可预测降级方式
- 模板文件、资质图片进入项目目录，可随完整备份迁移；项目副本保留这两类业务资产
- 新增资质图片上传、缩略图列表和逐张删除，使用 Pillow 验证真实图片内容
- Word 导出自动生成两列资质证明材料，图片等比缩放、内嵌并生成文件名图注
- Word 版本元数据新增模板文件名和资质图片数量，方便回溯交付依据
- 解析结果新增 `parser_engine`，旧项目 JSON 通过默认值保持兼容
- 新增 MinerU CLI 适配器，优先读取 `*_content_list.json` 保留页码，必要时回退最大 Markdown
- 新增独立安装脚本和 `.mineru-venv` Git 忽略，主应用在 MinerU 缺失或失败时继续可用
- 新增 OpenAI 兼容 Embedding 客户端，达到资料阈值后执行关键词与向量混合排序
- 文件向量缓存覆盖路径、修改时间、大小、接口和模型，知识资料变化时同步失效
- 新增模板与图片存储、模板导出、MinerU 自动/失败回退和混合检索缓存测试

### V1.4 正式投标 Word 版式重构

- 默认版式改为黑白灰正式投标风格，取消蓝色大标题、红色提示条和 AI 页脚
- 正文统一为宋体小四（12 磅）、固定值 26 磅行距，章节标题和分级标题统一黑体黑色
- 封面重做为项目名称、投标文件类型、招标人、投标人盖章位和日期的传统投标结构
- 页眉仅保留项目名称和“投标文件”，页码居中显示，封面继续使用首页不同
- 表格改为浅灰表头、黑色文字、细灰框线和白色正文单元格，取消深蓝表头、红色风险底纹和斑马色
- 表格按序号、页码、分值、状态等短字段分配窄列，叙述字段获得主要宽度；跨页重复表头并禁止单行拆分
- 导出前清理 Markdown 代码围栏、YAML frontmatter、水平分隔线、重复章节标题和裸露的 `[资料1]` 引用标记
- 每个独立有序列表重新从 1 开始，修复跨章节或跨段落延续编号问题
- 章节换页改为标题前分页，消除页尾分页符造成的整页空白
- 内部核对与复核附录改为可选项，默认不进入正式投标正文，复核结果仍保留在系统中
- 新增正式配色、内容清洗、列表重启和内部附录开关回归测试
- 使用江夏办公区屋面防水真实项目数据重新导出并由 Microsoft Word 渲染，逐页检查正文 17 页和内部附录长表 20 页

### V1.5 宋体与小四表格统一

- 正文、封面标题、章节标题和各级标题全部统一为宋体，保留粗体和字号层级区分
- 所有表格的表头和正文文字统一为宋体小四（12 磅），不再使用五号或更小字号
- 长项目名称按字符数动态降级字号，避免封面出现单字孤行
- 在不缩小表格字体的前提下压缩紧凑表格单元格上下留白和表内行距
- 可选内部复核长表保持重复表头和整行不拆分，并由 21 页收敛为 20 页
- 使用当前江夏项目重新执行 Microsoft Word 原生渲染，逐页确认正文 17 页和小四长表 20 页无裁切、重叠或竖排

### V1.6 Word 标题字体主题回退修复

- 修复标题虽然在 DOCX 中声明为宋体，但同时继承 `eastAsiaTheme`，导致 Microsoft Word 实际显示为 `MS Gothic` 的问题
- 清除字符、样式及文档默认字体中的 `asciiTheme`、`hAnsiTheme`、`eastAsiaTheme` 和 `cstheme` 属性
- 将正文、标题和表格的东亚字体显式固定为宋体，并将字符语言固定为 `zh-CN`
- 质量检查新增主题字体回退检测，自动测试覆盖文档默认样式、标题样式和表格直接格式
- Microsoft Word 实测默认版 41 个标题、内部复核版 46 个标题，非宋体标题均为 0
- 正式生成江夏项目 V005，Word 实测 17 页、41 个标题全部为宋体，表格全部为宋体小四（12 磅）
- V005 的 17 页 Word 原生渲染结果与逐页检查通过的 QA 文档逐像素一致，差异页为 0

### V1.7 内部复核版本与北京时间记录

- 明确区分正式投标版和内部复核版：正式版默认不附带内部材料，避免误交甲方；勾选内部附录后生成“内部核对事项”和“内部复核报告”
- 为当前江夏项目新增 V006 内部复核版，保留 V004、V005，不覆盖历史文件
- V006 经 Microsoft Word 验证共 20 页，包含内部核对事项和内部复核报告，46 个标题全部为宋体
- V006 的 64 个表格单元格全部为宋体小四（12 磅），20 页渲染结果与逐页检查通过的 QA 文档逐像素一致
- Word 新版本记录直接使用北京时间 `+08:00`；界面会将历史 UTC 版本时间统一转换为北京时间显示

### V1.8 用户操作与客户交付手册

- 新增 `用户操作与交付手册.md`，作为客户培训、日常操作、换机部署和交付验收的统一文档
- 按当前七个功能页逐项说明操作顺序、输入、输出、人工责任和常见风险
- 专门界定知识资料、资质图片、提交附件、Word 模板、自动复核、分析验收、Word 质检和提交包校验之间的区别
- 增加上游修改对下游结果的影响矩阵，明确何时必须重新分析、生成、复核、导出或打包
- 增加 Windows 新电脑安装、本地模型、MinerU、项目备份迁移、数据安全和客户交付验收清单
- README 首页和文档索引已增加操作手册入口

## 当前功能状态

| 环节 | 当前能力 | 人工职责 |
| --- | --- | --- |
| 项目管理 | 新建、复制、归档、恢复、完整备份和导入 | 决定项目命名和归档时机 |
| 招标文件 | 解析 PDF/DOCX/TXT/MD，支持自动/原生/MinerU 增强及失败回退 | 检查复杂表格和扫描页 |
| 分析确认 | 规则/LLM 提取、原文引用、自动基线和真实项目验收报告 | 逐项确认条款并记录漏项、误报和耗时 |
| 知识资料 | 三类资料、模板和资质图片管理；小规模关键词、大规模可选混合向量检索 | 提供真实、有效、最新的企业资料 |
| 章节生成 | 规则/LLM 草稿、证据来源、占位符和版本恢复 | 补齐事实、案例、参数和承诺 |
| 复核检查 | 自动发现遗漏、数值冲突和待补充项 | 处理高风险问题并确认最终内容 |
| Word 导出 | 默认 A4 精排版、真实模板占位适配、资质图片编排、版本记录和成品质检 | 核对模板占位、证书有效期和占位内容 |
| 提交清单 | 自动同步所需材料、附件分类、状态维护、断链提醒和 CSV 导出 | 准备真实附件并逐项确认签章、装订和提交状态 |
| 提交打包 | 结构预检、风险确认、版本化 ZIP、下载前完整性校验和校验报告 | 最终核对签字盖章、介质、密封及平台上传要求 |

## 数据与恢复

项目数据位于：

```text
D:\灵坤\投标系统\data\projects\<project_id>\
├─ source\
├─ parsed.json
├─ analysis.json
├─ analysis_baseline.json
├─ analysis_baseline_meta.json
├─ analysis_acceptance.json
├─ drafts.json
├─ review.json
├─ submission_checklist.json
├─ knowledge\
│  ├─ company\
│  ├─ product\
│  └─ history\
├─ template\
├─ qualification_images\
├─ attachments\
│  ├─ qualification\
│  ├─ business\
│  ├─ technical\
│  ├─ pricing\
│  ├─ signature\
│  └─ other\
├─ versions\drafts\
├─ versions\exports\
├─ versions\packages\
├─ output\
└─ packages\
```

项目 ZIP 备份是单项目迁移方式；整机灾备仍建议定期备份整个 `data` 目录。`.env` 包含模型接口配置，不进入 Git，也不包含在项目 ZIP 内。

## 验证记录

2026-08-05 当前本地验证：

- Python 静态编译通过
- `pip check` 无依赖冲突
- 82 项自动化测试全部通过
- 备份导出、异地导入、重复导入和篡改拦截已有自动化测试
- 示例项目已生成 6 个章节草稿和 20 个复核问题，其中 6 个高风险
- 示例 V3 Word 可被 `python-docx` 重新打开，并通过 A4、字体、字距、表格宽度、首页不同和表格禁止拆行等结构检查
- 示例 V3 使用 Microsoft Word 原生导出 PDF，并逐页检查 16 页 PNG，无文字裁切、重叠、表格拆行或页眉页脚错位
- 示例 V3 输出：`D:\灵坤\投标系统\data\demo\示例投标文件初稿V3.docx`
- 当前江夏项目正式版式 QA 输出：`D:\灵坤\投标系统\tmp\layout_audit_formal\formal.docx`
- 当前江夏项目已由 Microsoft Word 原生渲染并逐页检查 17 页；可选内部复核长表版本逐页检查 20 页
- 标题字体主题回退修复已由 Microsoft Word 验证：默认版 41 个标题、内部复核版 46 个标题、正式 V005 41 个标题，非宋体标题均为 0
- 正式修复版 V005：`D:\灵坤\投标系统\data\projects\proj_f94c835e9e37\output\江夏办公区2号楼（办公楼）屋面防水维修改造项目_投标文件初稿_V005.docx`
- V005 SHA-256：`377257db9ea716d05e8960c3c9169887eb03eee05932bc52c3b6530664007899`
- 内部复核版 V006：`D:\灵坤\投标系统\data\projects\proj_f94c835e9e37\output\江夏办公区2号楼（办公楼）屋面防水维修改造项目_投标文件初稿_V006.docx`
- V006 北京时间：`2026-08-05 14:00:54`，SHA-256：`e149b94c5af69e98f94ef7c2876e4d8d3a0e766c44ca04d27aa5c2f5e22d0203`
- Streamlit 本轮重启后健康检查通过 `200 / ok`
- `llama-server` 的 `/v1/models` 返回 `qwen3:4b`，鉴权中文对话约 1.65 秒返回“本地模型正常”
- 本地真实 LLM 招标分析使用 `analysis_mode=llm` 完成，未触发规则回退
- 页面已实际验证进度展示、备份下载、归档筛选、归档和恢复，测试项目已恢复原状态

## 已知边界

- MinerU 为可选独立环境，首次模型下载较大；复杂 PDF 表格和扫描件仍需人工复核
- 混合向量检索依赖外部 OpenAI 兼容 Embedding 服务，未配置时保持关键词模式
- Word 模板依赖明确占位符，不自动理解任意模板中每个空白区域的业务含义
- 项目归档用于整理列表，不是权限锁定；归档项目恢复后可继续编辑
- 单机本地模式不提供多人并发、审批、权限和审计日志
- 自动复核只能辅助发现问题，不能替代投标负责人和法务审查
- 命中率和覆盖率基于人工确认结果估算，样本量不足时不能作为通用模型结论

## 下一阶段

按单人开发收益排序：

1. 使用 2 至 3 份真实招标文件填充验收台账，对比漏项、误报和人工耗时。
2. 收集 1 至 2 套真实甲方 Word 模板，固化占位符并做逐页交付验收。
3. 扫描件成为高频瓶颈时运行 `setup_mineru.ps1`，观察准确率、耗时和磁盘占用。
4. 知识文件达到约 40 份后配置 Embedding 服务，使用真实问句评估混合检索命中率。

暂不扩展多用户、复杂权限、通用工作流、全网抓取和多 Agent 编排，避免单人开发被平台化工作拖住。
