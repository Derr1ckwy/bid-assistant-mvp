# 投标初稿助手用户操作与交付手册

版本：V1.0  
适用系统：投标初稿助手  
更新日期：2026-08-05  
适用对象：投标负责人、商务人员、技术方案编制人员、系统管理员和客户交付实施人员

---

## 1. 手册目的

本手册用于解决三类问题：

1. 用户知道每个功能在什么场景使用，以及它不能解决什么问题。
2. 用户能够按正确顺序完成从招标文件到 Word 初稿、提交清单和提交包的完整流程。
3. 实施人员能够把系统迁移到新电脑，并完成模型、数据、备份和交付验收。

系统定位是“可追溯的投标初稿辅助工具”，不是无人审核的一键出标系统。系统可以减少招标要求整理、资料检索、章节初稿编写、风险检查和材料打包工作，但不能替代投标负责人、技术负责人、商务负责人、法务或最终签章人员。

## 2. 使用前必须理解的边界

### 2.1 系统可以做什么

| 能力 | 主要作用 | 最终责任人 |
| --- | --- | --- |
| 招标文件解析 | 从 PDF、DOCX、TXT、Markdown 中提取可分析文本 | 用户检查扫描页、复杂表格和附件是否完整 |
| 招标要求分析 | 提取项目基本信息、强制要求、评分项、资格、材料、时间和风险 | 投标负责人逐项确认 |
| 知识资料检索 | 从企业、产品和历史方案资料中寻找章节编写依据 | 资料管理员保证资料真实、有效、最新 |
| 章节草稿生成 | 按章节计划生成规则草稿或 LLM 草稿 | 编制人员修改为真实、可交付内容 |
| 自动复核 | 查找遗漏、冲突、占位符、证据缺失和废标风险 | 投标负责人判断是否真正处理完成 |
| Word 导出 | 生成带版本记录和质量检查的 DOCX 初稿 | 用户完成最终校对、签章和甲方模板核验 |
| 提交清单 | 管理需要提交的材料、状态、页码和附件关联 | 商务人员准备并确认全部真实附件 |
| 提交包 | 汇总 Word、附件、清单和校验文件 | 提交人员完成最终平台上传或线下封装 |

### 2.2 系统不能替代什么

- 不保证自动识别结果百分之百正确。
- 不自动确认营业执照、资质证书、人员证书和业绩证明是否真实有效。
- 不自动完成报价、签名、盖章、加密、CA 签章或采购平台提交。
- 不自动理解任意甲方 Word 模板中的所有空白位置；模板需要明确占位符。
- 不提供多人账号、权限审批、在线协同编辑和审计追责。
- 不应把带“内部核对事项”或“内部复核报告”的版本直接提交给甲方。
- 不应把模拟企业资料直接用于真实投标。

## 3. 推荐工作流程

```mermaid
flowchart LR
    A[新建项目] --> B[上传并解析招标文件]
    B --> C[分析并人工确认]
    C --> D[准备知识资料/模板/资质图片]
    D --> E[调整章节计划并生成草稿]
    E --> F[人工修改正文]
    F --> G[生成复核报告并处理问题]
    G --> H[导出 Word 新版本]
    H --> I[同步提交材料清单]
    I --> J[上传并关联提交附件]
    J --> K[生成和校验提交包]
```

推荐原则：先确认招标要求，再生成正文；先处理正文和复核问题，再导出 Word；先保存材料清单，再生成提交包。

## 4. 容易混淆的功能界定

### 4.1 知识资料、资质图片、提交附件和 Word 模板

| 功能 | 放什么 | 用在什么环节 | 是否直接进入最终文件 |
| --- | --- | --- | --- |
| 企业资料 | 公司介绍、组织架构、资质清单、人员履历、财务概况、服务体系 | 章节生成时检索引用 | 不直接进入，先用于生成正文 |
| 产品资料 | 产品参数、材料选型、技术说明、施工工艺 | 技术章节生成 | 不直接进入，先用于生成正文 |
| 历史方案 | 类似项目方案、案例、实施经验 | 方案和业绩章节生成 | 不直接进入，先用于生成正文 |
| 资质图片 | 营业执照、证书、认证图片的 PNG/JPG/JPEG | Word 导出 | 会在 Word 末尾自动排成两列 |
| 提交附件 | 最终需要随标书提交的真实 PDF、DOCX、图片、报价或签章文件 | 提交清单和提交包 | 会进入最终 ZIP 提交包 |
| Word 模板 | 带占位符的甲方或公司 DOCX 模板 | Word 导出 | 决定 Word 的基础版式和内容插入位置 |

使用判断：用于“写正文”的放知识资料；用于“展示证书图片”的放资质图片；用于“最终随包提交”的放提交附件；用于“控制 Word 外观”的放 Word 模板。

### 4.2 自动复核、真实项目分析验收、Word 质检和提交包校验

| 功能 | 检查对象 | 回答的问题 |
| --- | --- | --- |
| 自动复核 | 已确认的分析结果和章节草稿 | 正文是否遗漏、冲突、缺证据或仍有占位符 |
| 真实项目分析验收 | 自动分析基线与人工确认结果 | 自动提取有多少命中、误报和漏项，节省了多少时间 |
| Word 质检 | 已生成的 DOCX 文件 | 文件是否损坏、被替换、缺章节或偏离系统版式 |
| 提交包校验 | 已生成的 ZIP 及包内文件 | ZIP 是否完整、安全，文件数量、大小和 SHA-256 是否一致 |

真实项目分析验收用于评估系统能力，不用于判断当前标书能否提交。自动复核用于发现内容风险，Word 质检用于检查文档文件，提交包校验用于检查最终文件集合。

### 4.3 复制项目、完整备份和草稿版本恢复

| 功能 | 适用场景 | 保留内容 |
| --- | --- | --- |
| 复制项目 | 同一招标文件制作不同方案分支，或基于旧项目建立新方案 | 复制源文件、分析、知识资料、草稿和复核状态；不复制旧 Word 和提交包历史 |
| 下载完整备份 | 换电脑、灾备、长期归档或交付迁移 | 包含项目源文件、分析、知识资料、版本、Word、附件、清单和提交包 |
| 恢复草稿版本 | 正文改坏、需要回到某次保存状态 | 只恢复章节草稿；恢复前自动保存当前草稿，原复核报告失效 |

### 4.4 正式 Word 和内部复核 Word

- “附带内部核对与复核附录”关闭：生成正式投标初稿，不把内部问题清单写入 Word。
- “附带内部核对与复核附录”开启：Word 末尾包含内部核对事项和内部复核报告，只能用于内部评审。
- 即使正式 Word 不包含内部报告，系统中的复核问题仍会保留，不会被删除。
- 每次导出都会产生新的 V001、V002、V003 版本，不覆盖历史文件。

## 5. 启动、停止和访问系统

### 5.1 正常启动

在项目目录打开 PowerShell：

```powershell
Set-Location -LiteralPath 'D:\灵坤\投标系统'
.\run.ps1
```

浏览器访问：

```text
http://127.0.0.1:8501
```

`run.ps1` 会检查项目虚拟环境，并在本地模型配置为 `localhost:11434` 时尝试启动本地模型服务。

### 5.2 停止系统

- 在运行 Streamlit 的 PowerShell 窗口中按 `Ctrl+C` 停止界面服务。
- 如需停止由项目管理的本地模型，执行：

```powershell
.\stop_local_llm.ps1
```

### 5.3 启动失败的快速检查

```powershell
Test-Path .\.venv\Scripts\python.exe
.\.venv\Scripts\python.exe -m pip check
Get-NetTCPConnection -LocalPort 8501 -ErrorAction SilentlyContinue
Get-NetTCPConnection -LocalPort 11434 -ErrorAction SilentlyContinue
```

## 6. 左侧项目管理

### 6.1 新建项目

1. 在“新项目名称”中输入便于识别的名称。
2. 点击“新建项目”。
3. 建议名称包含项目简称或采购编号，不要只写“测试”或“新项目”。

### 6.2 切换项目

在“当前项目”下拉框选择项目。所有七个功能页都属于当前项目，上传资料前必须先确认项目名称。

### 6.3 复制项目

适合以下情况：

- 同一招标项目需要制作技术路线 A/B 两个分支。
- 希望保留当前正文，同时进行大幅改写。
- 类似项目需要复用知识资料和章节结构。

复制后应检查项目名称、招标人、预算、截止时间和全部强制要求，不能直接沿用旧项目事实。

### 6.4 归档和恢复

归档只隐藏项目，不会删除数据，也不是权限锁定。勾选“显示已归档项目”后可以查看和恢复。

### 6.5 完整备份和导入

下载完整备份适用于换电脑和灾备。导入时系统会检查路径安全、文件数量、解压体积和 SHA-256。同一备份重复导入会生成新的项目 ID，不覆盖已有项目。

## 7. 模型配置

### 7.1 规则模式和 LLM 模式

| 模式 | 是否需要模型 | 优点 | 适用场景 |
| --- | --- | --- | --- |
| 规则模式/规则草稿 | 否 | 稳定、快速、离线可用 | 模型不可用、先做结构整理、敏感环境 |
| LLM 增强/LLM 生成 | 是 | 理解和表达能力更强 | 正常分析、章节初稿和复杂文本归纳 |

LLM 连接不可用时，系统会自动回退到规则模式。回退保证流程可继续，但生成质量通常低于可用的 LLM 模式。

### 7.2 本地模型配置

当前推荐配置：

```text
接口地址：http://localhost:11434/v1
模型名称：qwen3:4b
API Key：ollama
```

点击“检测连接”，显示可用后再点击“保存配置”。配置写入本机 `.env`，不会进入 Git 或项目备份。

### 7.3 云端模型配置

可以填写 OpenAI 兼容接口地址、模型名称和 API Key。使用云端模型前，客户必须确认招标文件和企业资料是否允许发送到外部服务。

### 7.4 分段参数

- 分段字符数：控制单次发送给模型的文本长度。普通项目建议保持默认值 12000。
- 最多分段：控制超长文件最多分析多少段。默认 12 段。
- 请求超时：本地模型较慢时可提高，普通场景保持 180 秒。

不要在不了解模型上下文限制时盲目增大分段字符数。

## 8. 第 1 页：招标文件

### 8.1 解析方式选择

| 解析方式 | 行为 | 推荐场景 |
| --- | --- | --- |
| 自动识别 | 先快速原生解析；扫描 PDF 文本过少时调用 MinerU | 默认选择 |
| 快速解析 | 只使用原生 PDF/DOCX/TXT/Markdown 解析 | 文字型 PDF、DOCX、追求速度 |
| MinerU 增强 | 优先使用 MinerU，失败时保留原生结果 | 扫描 PDF、复杂版面、原生文本明显缺失 |

### 8.2 正确操作

1. 选择解析方式。
2. 上传招标文件。
3. 点击“保存并解析”。
4. 检查字符数、页数、扫描件风险、解析引擎和警告。
5. 展开“查看解析文本”，抽查项目名称、金额、日期、评分表和强制要求。

### 8.3 必须人工检查的内容

- PDF 中的复杂表格是否被按正确顺序读取。
- 扫描页是否存在空白或乱码。
- 附件、补遗、澄清文件是否已经合并考虑。
- 页码是否与原文引用基本一致。

替换招标文件后，必须重新执行分析确认、章节生成、复核检查和 Word 导出。旧结果不会自动代表新文件。

## 9. 第 2 页：分析确认

### 9.1 开始分析

- 规则模式无需模型，适合快速建立结构。
- LLM 增强适合正式分析；模型不可用时自动回退并显示原因。

开始分析后，系统保存一份“自动分析基线”，用于后续真实项目分析验收。

### 9.2 项目基本信息

逐项确认项目名称、招标人/采购人、代理机构、预算/最高限价和投标截止时间。原文没有明确内容时，应保持为空或人工确认，不能凭经验填写。

### 9.3 要求表格

| 状态 | 含义 | 后续影响 |
| --- | --- | --- |
| 待确认 | 系统提取但尚未人工确认 | 导出时会产生风险提示 |
| 已确认 | 已核对原文并认可 | 可作为章节和复核依据 |
| 忽略 | 确认该条不是本项目有效要求 | 不应继续作为投标依据 |
| 待核对 | 需要其他人员或原文补充确认 | 仍属于未闭环事项 |

强制要求和废标风险优先级最高，其次是资格、所需材料、时间节点和评分项。

### 9.4 保存人工确认结果

编辑完成后必须点击“保存人工确认结果”。未保存的界面修改不会成为后续章节生成的正式输入。

保存分析修改后，原复核报告会失效。已生成草稿不会自动重写，重要分析内容发生变化时必须重新生成或人工修改草稿。

### 9.5 真实项目分析验收

此功能用于统计自动分析质量和节省时间，不是标书放行审批。

操作顺序：

1. 保留自动分析基线。
2. 人工逐项确认、删除误报并补充漏项。
3. 保存人工确认结果。
4. 填写验收人、纯人工预计耗时、系统协助实际耗时和备注。
5. 保存并下载验收报告。

覆盖率只有在人工复核完成后才有参考价值。历史项目以当前结果建立的基线，只能统计建立之后的变化。

## 10. 第 3 页：知识资料

### 10.1 Word 模板

支持一个当前项目模板。上传新模板会替换旧模板。

可用占位符：

```text
{{PROJECT_NAME}}
{{PURCHASER}}
{{AGENCY}}
{{BUDGET}}
{{BID_DEADLINE}}
{{GENERATED_DATE}}
{{BID_CONTENT}}
```

`{{BID_CONTENT}}` 应单独放在一个段落中，作为正文插入位置。没有该占位符时，正文会追加到模板末尾。

模板负责基础版式，不负责判断甲方每个空白表格应填写什么业务内容。首次使用新模板必须生成测试版并逐页检查。

### 10.2 资质图片

支持 PNG、JPG、JPEG。建议文件名使用真实证书名称，例如：

```text
营业执照.png
建筑业企业资质证书.jpg
质量管理体系认证证书.jpg
```

Word 导出时，图片会在末尾按两列等比缩放，并使用文件名生成图注。证书有效期、清晰度、方向和是否需要打码仍需人工检查。

### 10.3 企业、产品和历史方案资料

支持 PDF、DOCX、TXT、Markdown、CSV、JSON。

- 企业资料：公司事实、人员、资质、财务、组织和服务能力。
- 产品资料：产品参数、材料性能、技术路线和施工工艺。
- 历史方案：类似项目方案、案例、风险处置和交付经验。

上传后不会自动改写已有草稿。资料发生变化时，需要重新生成相关章节或人工更新正文。

### 10.4 关键词检索和向量检索

- 知识文件较少时使用关键词检索，部署简单、无需额外模型。
- 文件达到 `VECTOR_MIN_FILES` 阈值且配置 Embedding 服务后，系统启用关键词和向量混合排序。
- 当前默认阈值为 40 个知识文件。
- 未配置 Embedding 时不会影响基础功能。

向量检索提高的是“找资料”的能力，不会自动判断资料真假，也不会替代资料分类和维护。

## 11. 第 4 页：章节生成

### 11.1 章节计划

章节计划决定生成哪些章节以及每章的编写要求。可以新增、删除、改名、取消选择或补充说明。

保存章节计划后再生成。章节标题和编写要求越具体，资料检索和生成结果越稳定。

### 11.2 规则草稿和 LLM 生成

- 规则草稿适合建立章节结构、明确缺失项和离线运行。
- LLM 生成适合形成较完整的初稿表达。
- 两种方式都可能保留“待补充”“待确认”等占位内容。

生成时，系统会按章节名称和编写要求检索知识资料，并记录引用资料名称。

### 11.3 草稿编辑

正文使用 Markdown 编辑。保存后自动建立草稿版本快照，并使旧复核报告失效。

重点检查：

- 企业名称、人员、业绩和证书是否真实。
- 工期、金额、质保期、日期和数量是否与招标文件一致。
- 是否仍有模拟数据、占位符或不适用于本项目的通用描述。
- 是否针对评分项提供了可得分的具体响应。

### 11.4 草稿版本恢复

每个项目最多保留最近 50 个草稿版本。恢复前系统会自动保存当前正文。恢复后必须重新生成复核报告。

## 12. 第 5 页：复核检查

点击“生成或刷新复核报告”后，系统检查：

- 缺失章节和未确认要求。
- 高风险强制条款和废标风险。
- 工期、质保期、金额、日期、人员数量和资质有效期冲突。
- 待补充、待确认、占位符和证据缺失。
- 评分项或必交材料未覆盖。

处理状态：

- 待处理：问题尚未闭环。
- 已处理：已在正文、资料或实际材料中解决。
- 忽略：确认该提示不适用于当前项目，并承担人工判断责任。

“保存复核状态”只记录处理结论，不会自动修改正文。正文需要由用户在章节页实际修改。

分析、章节计划或正文修改后，应重新生成复核报告。

## 13. 第 6 页：Word 导出

### 13.1 导出前检查

系统会显示项目基本信息、章节完整性、高风险、占位符、证据追溯等检查项。

- “不可导出”必须先处理。
- “需确认”允许内部生成，但必须勾选风险确认。
- 风险确认不等于问题已解决，只表示本次文件仅用于内部复核。

### 13.2 模板和内部附录

- 已上传模板时，可以选择是否使用项目模板。
- 正式提交前建议关闭“附带内部核对与复核附录”。
- 内部评审时可以开启该选项，生成包含风险明细的版本。

### 13.3 版本说明

建议填写明确说明，例如：

```text
第一次内部评审
已补充施工组织和质保承诺
甲方模板适配测试版
正式提交候选版
```

### 13.4 Word 版本记录

- 版本号从 V001 递增，不覆盖历史版本。
- 记录生成时间、章节数、待处理数、高风险数、模板、资质图片数量和 SHA-256。
- 生成时间和质检时间按北京时间显示。

### 13.5 Word 质检结果

- 通过：结构和系统版式检查通过。
- 提示：文件可打开，但仍有占位符、模板差异或需人工确认事项。
- 不通过：文件损坏、哈希变化、缺章节、非 A4 或结构异常，系统停止下载。

不要直接修改项目 `data\projects\...\output` 中的历史 Word 文件。历史文件被外部修改后 SHA-256 会变化，系统会将其判定为被替换。

如需在 Microsoft Word 中人工深化，请下载副本后编辑。当前版本尚不支持把外部修改后的 Word 重新导入并登记为系统版本，因此外部终稿需要单独管理或手工打包。

## 14. 第 7 页：提交清单和提交包

### 14.1 从分析结果生成清单

首次点击“从分析结果生成清单”。分析内容更新后点击“同步招标材料”。同步后仍需人工确认每一项是否必交。

材料清单本身不需要上传。它是系统内维护的一张表，由以下两种方式产生：

1. 从“分析确认”中的“所需材料”自动同步。
2. 当分析没有识别到材料时，在清单表格中手工新增行并保存。

RAG 知识资料不会自动变成材料清单，也不会自动变成提交附件。清单描述“要交什么”，提交附件才是“实际交付的文件”。

### 14.2 提交附件分类

系统分类包括资格、商务、技术、报价、签章装订和其他。附件是最终提交文件，不等同于知识资料。

### 14.3 清单字段

| 字段 | 用途 |
| --- | --- |
| 类别 | 对材料进行业务分类 |
| 材料名称 | 明确需要提交的文件或证明 |
| 页码 | 记录招标文件来源页码 |
| 必交 | 决定是否阻止打包 |
| 状态 | 待准备、已备妥或不适用 |
| 关联附件 | 把清单项绑定到已上传的真实附件 |
| 备注 | 记录签章、份数、原件、密封等要求 |

删除附件后，原清单关联会变成断链并阻止打包。

### 14.4 同步结果为 0 项时怎么操作

以“江夏办公区2号楼”当前项目为例，分析结果中的“所需材料”为空，因此点击“从分析结果生成清单”后会得到 0 项。这不是要求上传一份材料清单文件，而是要求人工建立系统内清单：

1. 在“最终提交材料清单（系统内填写，无需上传清单文件）”表格右上角点击“Add row”。
2. 先填写材料名称、类别、是否必交和备注，状态保持“待准备”。
3. 点击“保存清单”。此时清单已经建立，但必交项仍未完成，所以仍不可打包。
4. 在“提交附件（真实文件）”选择类别，上传对应的营业执照、资质证书、授权书、报价表等真实文件，然后点击“保存附件”。
5. 回到清单表格，在“关联附件”列选择刚上传的文件。
6. 实际材料检查完成后，把状态改成“已备妥”；确实不适用的材料改成“不适用”并在备注中写明依据。
7. 再次点击“保存清单”。

建议先建立以下最小清单，再根据真实招标文件增删：

| 类别 | 材料名称 | 必交 | 初始状态 | 对应上传位置 |
| --- | --- | --- | --- | --- |
| 资格文件 | 营业执照复印件 | 是 | 待准备 | 提交附件 / 资格文件 |
| 资格文件 | 建筑业企业资质证明 | 是 | 待准备 | 提交附件 / 资格文件 |
| 商务文件 | 法定代表人授权委托书 | 是 | 待准备 | 提交附件 / 商务文件 |
| 技术文件 | 技术响应文件 | 是 | 待准备 | 提交附件 / 技术文件 |
| 报价文件 | 报价响应表 | 是 | 待准备 | 提交附件 / 报价文件 |
| 签章与装订 | 签字盖章及装订检查 | 是 | 待准备 | 提交附件 / 签章与装订 |

以上只是流程示例，不能代替真实招标文件中的材料要求。真实项目必须逐页核对资格、报价、签章、份数、密封和电子平台上传要求。

### 14.5 保存和导出清单

编辑后必须点击“保存清单”。有未保存修改时不能生成提交包。CSV 清单带 UTF-8 BOM，可直接用 Excel 打开。

### 14.6 生成提交包

1. 选择要打包的 Word 版本。
2. 确认清单已保存。
3. 处理待准备的必交项和附件断链。
4. 查看 Word 质检和复核风险。
5. 填写提交包版本说明。
6. 点击“生成提交包”。

存在复核风险时，只能在确认后生成“仅内部预审”包，不能把该标记理解为正式提交放行。

常见预检结果含义：

| 检查项 | 不通过原因 | 处理方法 |
| --- | --- | --- |
| Word 版本 | 尚未生成 Word | 先到第 6 页生成并选择 Word 版本 |
| 材料清单 | 系统内清单为空 | 从分析同步，或手工新增清单行并保存；无需上传清单文件 |
| 清单保存状态 | 表格有未保存修改 | 点击“保存清单” |
| 必交项 | 至少一个必交项仍为待准备 | 准备材料后改为已备妥，或有依据地标为不适用 |
| 附件关联 | 清单关联的附件被删除或不存在 | 重新上传并关联正确附件 |
| 复核状态 | 仍有待处理风险 | 处理风险；暂未处理时只能确认生成内部预审包 |

### 14.7 提交包内容

```text
投标文件/
提交附件/
最终提交材料清单.csv
提交包说明.txt
package-manifest.json
```

提交包版本号从 P001 递增，最多保留最近 20 版。下载前系统会重新校验 ZIP 路径、文件大小、文件数量和 SHA-256。

## 15. 上游修改对下游结果的影响

| 修改动作 | 必须重新执行的后续步骤 |
| --- | --- |
| 更换或重新解析招标文件 | 分析确认、章节生成、复核、Word、提交清单和提交包 |
| 修改并保存分析结果 | 检查/重生成章节、重新复核、重新导出 Word |
| 修改章节计划 | 重新生成相关章节、重新复核、重新导出 Word |
| 修改或恢复草稿 | 重新复核、重新导出 Word |
| 上传或删除知识资料 | 重新生成受影响章节，或人工修改正文 |
| 更换 Word 模板 | 生成新的 Word 测试版本并逐页检查 |
| 增删资质图片 | 生成新的 Word 版本 |
| 更新复核状态 | 如正文有变化，应先保存正文并重新复核 |
| 更新提交附件或清单 | 保存清单并重新生成提交包 |

不要依赖项目顶部的流程进度条判断内容已经正确。进度条表示相应数据文件已经存在，不代表人工审核已经完成。

## 16. 新电脑部署

### 16.1 建议硬件和系统

| 项目 | 建议配置 |
| --- | --- |
| 操作系统 | Windows 10/11 64 位 |
| 内存 | 最低 16GB，复杂 PDF 和 MinerU 建议 32GB |
| 显卡 | 本地 Qwen3 4B 建议支持 Vulkan，8GB 显存更稳妥 |
| 磁盘 | 至少预留 20GB；安装 MinerU 时需要更多空间 |
| Python | 3.11 或 3.12 64 位 |
| 办公软件 | 建议安装 Microsoft Word，用于最终版式和字体检查 |
| 网络 | 首次安装依赖、模型或使用云端 API 时需要 |

没有合适显卡时，可以使用规则模式，或配置经过客户批准的 OpenAI 兼容云端模型。

### 16.2 安装项目

将项目源代码放到目标目录后执行：

```powershell
Set-Location -LiteralPath 'D:\灵坤\投标系统'
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
```

`.venv` 不应直接从旧电脑复制，新电脑应重新创建。

### 16.3 安装本地模型

```powershell
.\setup_local_llm.ps1
.\start_local_llm.ps1
```

安装完成后检查：

```powershell
Invoke-RestMethod `
  -Uri 'http://127.0.0.1:11434/v1/models' `
  -Headers @{Authorization='Bearer ollama'}
```

### 16.4 可选安装 MinerU

MinerU 使用独立环境：

```powershell
.\setup_mineru.ps1
```

安装前需要可用的 `uv`。MinerU 模型较大，只有扫描文件确实成为瓶颈时才建议安装。

### 16.5 启动并验收

```powershell
.\run.ps1
```

打开 `http://127.0.0.1:8501`，完成本手册第 18 章的交付验收。

## 17. 数据迁移、备份和安全

### 17.1 推荐迁移方式

单个项目迁移：

1. 旧电脑进入“项目管理”。
2. 下载当前项目完整备份 ZIP。
3. 将 ZIP 安全复制到新电脑。
4. 新电脑点击“导入备份”。
5. 检查源文件、知识资料、草稿版本、Word、附件和提交包。

整机迁移：在系统停止运行后备份整个 `data` 目录。恢复前建议先备份新电脑已有的 `data` 目录。

### 17.2 不包含在项目备份中的内容

- `.env` 中的 API Key 和模型配置。
- `.venv` 和 `.mineru-venv` 虚拟环境。
- `models` 本地模型文件。
- `runtime` 模型运行时和日志。
- Git 历史和源代码更新记录。

### 17.3 安全注意事项

- 系统默认只监听 `127.0.0.1`，不要直接暴露到公网。
- 当前没有账号密码和权限系统，不适合多人共享服务器直接开放。
- 云端模型会产生数据外发，必须经过客户授权。
- `.env` 不应通过邮件、聊天群或项目备份发送。
- 真实招标文件、企业资料和人员信息应按客户数据安全制度保存。
- 定期备份 `data`，并抽查备份是否能在另一台电脑导入。

## 18. 客户交付验收清单

### 18.1 基础环境

- [ ] Windows、Python 和 PowerShell 版本符合要求。
- [ ] 项目虚拟环境创建成功，依赖安装无冲突。
- [ ] `run.ps1` 可以启动，浏览器能访问 8501 端口。
- [ ] 本地模型或客户批准的云端模型连接检测通过。
- [ ] Microsoft Word 能打开系统导出的 DOCX。

### 18.2 功能验收

- [ ] 能创建、复制、归档、恢复项目。
- [ ] 能上传并解析一份 PDF 或 DOCX 招标文件。
- [ ] 能运行规则分析和 LLM 增强分析。
- [ ] 项目信息和要求表格可以人工编辑并保存。
- [ ] 能上传、查看和删除三类知识资料。
- [ ] 能上传 Word 模板和资质图片。
- [ ] 能生成章节、保存修改和恢复草稿版本。
- [ ] 能生成复核报告并保存问题状态。
- [ ] 能生成正式 Word 和内部复核 Word。
- [ ] Word 标题、正文和表格使用宋体，表格文字为小四 12 磅。
- [ ] Word 版本时间和质检时间显示为北京时间。
- [ ] 能同步材料清单、上传附件并建立关联。
- [ ] 能生成提交包并通过完整性校验。

### 18.3 迁移和恢复验收

- [ ] 旧电脑项目可以导出完整备份。
- [ ] 新电脑可以导入备份且项目内容完整。
- [ ] API Key 未进入项目 ZIP。
- [ ] 至少完成一次 `data` 目录离线备份。
- [ ] 客户已明确备份责任人、周期和存放位置。

### 18.4 交付资料

- [ ] 项目源代码或约定的安装包。
- [ ] 本手册。
- [ ] `.env.example` 配置样例，不包含真实密钥。
- [ ] 客户专用 Word 模板及占位符说明。
- [ ] 客户真实企业资料清单。
- [ ] 模型来源、模型文件 SHA-256 和许可证说明。
- [ ] 当前已知边界和后续维护联系人。

## 19. 常见问题

### 19.1 上传招标文件后项目信息没有自动填入

“保存并解析”只提取文本，不执行招标要求分析。解析完成后，需要进入“2. 分析确认”点击“开始分析”。

### 19.2 LLM 不可用但系统仍生成了内容

系统在模型连接失败时会回退到规则模式。页面会显示回退原因，应把该版本视为结构草稿并加强人工修改。

### 19.3 为什么上传知识资料后正文没有变化

知识资料只在章节生成时检索。已有正文不会自动重写，需要重新生成相关章节或人工修改。

### 19.4 为什么 Word 没有内部复核报告

默认关闭“附带内部核对与复核附录”，用于避免内部风险信息进入正式投标文件。内部评审时需要主动开启该选项并生成新版本。

### 19.5 为什么 Word 下载被禁用

常见原因包括文件被替换、SHA-256 不一致、DOCX 损坏、章节缺失、非 A4 或必要结构缺失。展开错误信息并下载质检报告定位。

### 19.6 材料清单需要我上传吗

不需要上传清单文件。先点击“从分析结果生成清单”；如果系统提示同步 0 项，说明分析结果没有识别到“所需材料”，需要在下方清单表格中手工新增材料名称、类别、是否必交和状态，然后点击“保存清单”。营业执照、报价表、授权书等真实文件应在“提交附件（真实文件）”区域上传，再在清单中关联。

### 19.7 为什么提交包按钮不可用

检查是否存在未保存清单、待准备必交项、附件断链、缺少 Word 版本或 Word 质检失败。

### 19.8 删除知识资料后为什么生成结果没有自动删除

知识资料和草稿是两个独立阶段。删除资料不会回滚历史草稿和 Word，需要人工判断受影响章节并重新生成。

### 19.9 能否直接修改系统 output 目录中的 Word

不建议。系统会通过 SHA-256 检查历史文件是否被替换。应下载副本后编辑，并在系统外妥善管理人工终稿。

### 19.10 能否多人同时使用

当前是单机单用户模式，没有账号、权限和并发编辑保护。多人协作应通过明确的文件交接和项目负责人机制完成。

## 20. 日常使用建议

### 每个新项目开始时

1. 创建独立项目。
2. 立即下载一次空项目备份不是必须的，完成分析确认后做首次备份更有价值。
3. 核对模型配置和数据是否允许外发。
4. 收集招标文件、补遗、企业资料、模板和真实附件。

### 每次内部评审前

1. 保存分析、章节和复核状态。
2. 生成带明确版本说明的内部复核 Word。
3. 下载 Word 质检报告。
4. 下载完整项目备份。

### 正式提交前

1. 关闭内部复核附录。
2. 处理全部高风险、强制要求和待补充内容。
3. 用 Microsoft Word 逐页检查字体、表格、图片、页码和模板。
4. 确认真实附件、签章、份数、密封和上传要求。
5. 生成新的正式候选 Word 和提交包。
6. 下载并留存 Word 质检报告、提交包校验报告和完整项目备份。

---

本手册描述的是当前已实现功能。客户交付前如增加权限、在线部署、外部 Word 回传、审批流或新的模型服务，应同步更新本手册和交付验收清单。
